import os

content = """# [월간 데이콘] 스마트 창고 출고 지연 예측 AI 경진대회

---

## 1. 배경
**AMR(자율이동로봇)** 기반 스마트 물류창고가 빠르게 확산되고 있습니다. 하지만 로봇 대수, 주문량, 배터리 상태, 통로 혼잡도 등 다양한 운영 요인이 복합적으로 작용하면서, **출고 지연을 사전에 예측하고 대응하는 것**은 여전히 어려운 과제입니다.

이번 대회에서는 가상의 스마트 물류창고 시뮬레이션에서 생성된 **운영 스냅샷 데이터**를 활용하여, 현재 시점으로부터 **향후 30분간의 평균 출고 지연 시간**을 예측하는 AI 모델을 개발합니다.

> **데이터 특징**
> - **시나리오:** 12,000개의 독립적인 창고 운영 시나리오
> - **시간 구성:** 각 시나리오별 약 6시간(25개 타임슬롯, 15분 간격)
> - **복합 요인:** 주문 유입, 로봇 운영, 배터리/충전, 혼잡도, 패킹 병목 등

---

## 2. 주제
**스마트 물류창고 운영 데이터를 기반으로 향후 30분간의 평균 출고 지연 시간(분)을 예측하는 모델 개발**

---

## 3. 설명
참가자는 창고 운영 스냅샷 데이터를 분석하여, 각 시점에서 향후 30분간 출고되는 주문들의 평균 지연 시간(`avg_delay_minutes_next_30m`)을 예측해야 합니다.

### 제공 데이터 구성
| 파일명 | 설명 | 주요 내용 |
| :--- | :--- | :--- |
| **train.csv / test.csv** | 메인 운영 데이터 | 로봇 상태, 주문량, 배터리, 혼잡도 등 **90개 피처** |
| **layout_info.csv** | 보조 테이블 | 창고 레이아웃 정보 |

---

## 4. 핵심 과제
* **비선형 패턴 분석:** 다양한 운영 요인이 복합적으로 작용하여 발생하는 비선형적인 지연 패턴 파악
* **데이터 통합:** 메인 테이블과 보조 테이블(레이아웃 정보)의 효율적인 결합 및 활용
* **예측 성능 최적화:** 시뮬레이션 데이터를 기반으로 한 실시간성 지연 시간 예측

---

**데이커 여러분의 EDA 능력과 모델링 실력으로 이 복합적인 물류 병목 현상을 예측해 보세요!**
"""

file_path = "smart_warehouse_delay_prediction_overview.md"

with open(file_path, "w", encoding="utf-8") as f:
    f.write(content)

print(f"File saved to: {file_path}")